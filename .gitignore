# Swift Package Manager build output
.build/

# Packaging artifacts (built by scripts/make-app.sh + make-dmg.sh)
DeyeWidget.app/
*.dmg

# macOS
.DS_Store

# Images (scratch); the committed screenshot is whitelisted below
*.png
!docs/screenshot.png
